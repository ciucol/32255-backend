const arr = [2, 4, 6, 8]

const response = arr.map(num => num + 1)

console.log(response)

// toma un array
// itera en cada elemento del array
// a cada elemento del array le aplica una función
// guarda el nuevo elemento en un nuevo array
// retorna el nuevo array